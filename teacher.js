const SB='https://kkvrthngjbmkkwsqcmbx.supabase.co',KEY='sb_publishable_oPqCSUdt9cCNQJYhbAyUzw__4LMAOAX';const $=s=>document.querySelector(s);function headers(){return{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json'}}async function load(){sessions.innerHTML='<section class="panel">결과를 불러오는 중입니다.</section>';let r=await fetch(`${SB}/rest/v1/rpc/get_korean_dashboard_web`,{method:'POST',headers:headers(),body:'{}'});if(!r.ok){sessions.innerHTML=`<section class="panel"><b>결과를 불러오지 못했습니다.</b><p class="help">대시보드 조회 권한을 확인해 주세요. (${r.status})</p></section>`;return}let rows=await r.json();sessions.innerHTML=rows.length?rows.map(x=>{let resp=x.responses||[];return `<section class="panel"><div class="taskhead"><b>${esc(x.family_name||'')}${esc(x.given_name||'')}</b><span>${esc(x.birth_year||'')} · ${esc(x.residence_country||'')} · ${x.completed_at?new Date(x.completed_at).toLocaleString('ko-KR'):''}</span></div>${resp.map(a=>`<div class="teacher-response"><span class="tag">${esc(a.domain||'')}</span><b>${esc(a.prompt||'')}</b><p>${a.paper?'✍️ 종이 작성 — Meet 화면에서 교사가 확인':esc(String(a.response??''))}</p>${a.type==='speech'?'<small>🎙️ 음성 인식문 · 실제 발음/유창성은 교사가 발화를 함께 검토</small>':''}</div>`).join('')}</section>`}).join(''):'<section class="panel">아직 제출된 진단이 없습니다.</section>'}function esc(v){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}document.querySelector('#refresh')?.addEventListener('click',load);load();